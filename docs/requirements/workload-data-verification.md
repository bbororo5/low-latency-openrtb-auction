# 부하·데이터·검증 기준

상태: 기준선 3.0

상위 문서: [제품·도메인 요구사항](product-domain-requirements.md)

이 문서는 어떤 부하, 데이터, 장애에서 무엇을 통과로 볼지 정한다. 복제본 수, 부하 분산, 저장소는 정하지 않는다.

## 1. 기준의 성격

- 일 1천만 경매는 특정 회사의 실측치가 아니라 포트폴리오 사업 가정이다.
- 일일 물량의 50%가 가장 바쁜 4시간에 모인다는 것도 재현을 위한 가정이다.
- 50ms는 정상 경로의 내부 목표고, 1,000 RPS는 예상 정점이 아니라 순간 부하·회복 시험이다.
- OpenRTB는 CPM과 낙찰·과금 통지를 구분한다. [IAB OpenRTB 2.6](https://github.com/InteractiveAdvertisingBureau/openrtb2.x/blob/main/2.6.md#423---object-bid)
- OpenRTB가 제시하는 웹 브라우저 노출 만료의 경험적 기준은 1분이다. [IAB OpenRTB 노출 만료 지침](https://github.com/InteractiveAdvertisingBureau/openrtb2.x/blob/main/implementation.md#72---impression-expiration)
- 180ms는 국내 S2S 경매 제한 시간의 참고값이다. [Kakao AdX 입찰 요청 규격](https://kakaobusiness.gitbook.io/main/partner/kakaoadx_rtb/spec#object-bid-request)

## 2. 부하

```text
일평균 = 10,000,000 / 86,400 = 115.74 RPS
바쁜 시간 평균 = 5,000,000 / 14,400 = 347.22 RPS
설계 정점 = 400 RPS
```

| 구분 | 공급자 요청률 | 지속 시간 | 의미 |
|---|---:|---:|---|
| 일평균 | 115.74 RPS | 24시간 환산 | 사업 물량 |
| 설계 정점 | 400 RPS | 바쁜 4시간 | 예상 수요 |
| 정상 합격 | 500 RPS | 10분 | 정점 대비 25% 여유 |
| 순간 부하 | 1,000 RPS | 60초 | 정확성과 회복 검증 |

경매당 자사 DSP 1개와 외부 DSP 2개를 호출한다.

| 공급자 RPS | SSP의 DSP 호출/초 | 자사 DSP 요청/초 |
|---:|---:|---:|
| 400 | 1,200 | 400 |
| 500 | 1,500 | 500 |
| 1,000 | 3,000 | 1,000 |

500 RPS에서 자사 DSP는 초당 캠페인 검색 500건, 잠재 지출 확보 약 300건, 낙찰 100건, 과금 90건을 처리한다.

## 3. 데이터

| 데이터 | 규모·분포 |
|---|---|
| 공급자 | 1,000개 |
| 게재 위치 | 10,000개, 상위 20%가 요청 80% 처리 |
| 광고주 | 10,000개, KRW |
| 캠페인 | 100,000개, 활성 30,000개 |
| 소재 | 200,000개, 캠페인당 평균 2개 |
| 활성 후보 | 요청당 p50 20 / p95 200 / p99 1,000 |
| 예산 편향 | 상위 1% 광고주에 금액 연산의 20% 집중 |
| 소진 임박 | 광고주·활성 캠페인 1%는 노출 1~10건 금액만 남김 |

입찰 CPM은 500, 1,000, 1,500, 2,000, 3,000 KRW를 사용한다. 광고주 충전액은 100만~1억 KRW, 캠페인 일일 한도는 10만~500만 KRW다. SSP의 과금 가능 시간은 낙찰 확정부터 60초며 DSP는 자체 시간 만료만으로 잠재 지출을 해제하지 않는다.

## 4. 요청과 장애 구성

정상 요청은 배너 300×250, 광고 영역 1개, KRW, 1차 가격이며 SSP는 `imp.exp=60`을 전달한다. k6가 공급자 요청과 렌더링 통지를 생성한다. 자사 DSP는 60%에 입찰하고 전체 경매의 20%를 낙찰한다. 낙찰의 90%는 60초 안에 렌더링되어 과금되고 10%는 미과금으로 확정된다.

| 조건 | 주입 | 기대 결과 |
|---|---:|---|
| 중복 공급자 요청·렌더링 통지·DSP 통지 | 해당 사건의 5% | 경매·예약·금액 효과 1회 |
| 과금 후 낙찰 도착 | 자사 낙찰의 1% | 동일한 최종 상태 |
| 자사 DSP 입찰 응답 유실 | 자사 입찰의 1% | SSP 미접수 확인 후 잠재 지출 해제 |
| 최종 결과 전달 실패 | 자사 최종 결과의 5% | 예약 유지, 복구 후 동일 결과 접수 |
| 패찰·과금 충돌 결함 주입 | 격리 시험에서만 주입 | 금액 재변경 없이 `MONEY_EVENT_CONFLICT` |
| 렌더링 통지 지연 | 기한 직전·직후에 동일 비율 주입 | SSP 최초 접수 시각으로 결정적 판정 |
| 위조·잘못 연결된 렌더링 통지 | 렌더링 통지의 5% | 과금 거부 |
| 외부 DSP 오류 | 요청의 5% | 다른 DSP로 경매 지속 |
| 외부 DSP 시간 초과 | `tmax + 300ms` 후 응답 | 늦은 입찰 제외 |
| 잘못된 입찰 | 외부 DSP 입찰의 5% | 낙찰 후보에서 제외 |

정상 부하시 SSP는 모순된 최종 결과를 0건 생성해야 한다. 충돌은 정상 분포가 아니라 계약 위반 방어를 확인하는 별도 결함 주입이다.

## 5. 목표

| 구분 | 목표 |
|---|---|
| 경매 제한 시간 | 기본 `tmax=180ms` |
| 정상 경로 | 500 RPS에서 p99 ≤ 50ms |
| DSP 시간 초과 | 400 RPS에서 p99 ≤ 200ms |
| 순간 부하 | 1,000 RPS에서 p99 ≤ 180ms |
| 최종 정산 전달 | 정상 상황에서 SSP 확정부터 DSP의 복구 가능한 접수까지 p99 ≤ 5초 |
| SSP 인스턴스 중단 | 전체 중단 0, 10초 안에 성공률 99.9% 회복 |
| 자사 DSP 인스턴스 중단 | 전체 중단 0, 10초 안에 성공률 99.9% 회복 |
| AZ 중단 | 전체 중단 0, 30초 안에 성공률 99.9% 회복 |
| 인정된 금액 효과 | RPO 0, 유실·중복 0건 |
| 과부하 회복 | 부하 감소 후 30초 안에 정상 범위 회복 |

전체 중단 0은 장애 구간의 어느 1초 구간도 성공률 0%가 되지 않는다는 뜻이다. 장애 순간의 처리 중 요청 일부는 실패할 수 있다.

## 6. 검증 환경

| 항목 | 기준 |
|---|---|
| 클라우드 | AWS `ap-northeast-2`, 시험 후 제거 |
| 장애 영역 | 최소 2개 AZ |
| 애플리케이션 비교 단위 | Linux x86_64, 2 vCPU, 8GiB |
| 부하 발생기 | 대상과 분리된 호스트 |
| 공급자 역할 | k6와 통합 시험이 공급자 요청·렌더링 성공·지연·유실·재시도를 생성 |
| 네트워크 | 사설 IP, AZ 간 경로 포함, 첫 버전에서 TLS 비용 제외 |
| 초기 통신 | HTTP POST + JSON |

정확한 복제본, 부하 분산, 저장소, 복제 구조는 ADR 후에 확정한다.

## 7. 검증 목록

| ID | 시험 | 합격 기준 |
|---|---|---|
| `검증-01` | 도메인 정확성 | 동일 경매·DSP 요청의 결과 재사용, 낙찰·캠페인·금액 규칙 100%, 예산 위반 0건 |
| `검증-02` | 500 RPS, 10분 | 실패·누락·잘못된 낙찰 0건, p99 ≤ 50ms |
| `검증-03` | 400 RPS, DSP 오류·시간 초과 | 공급자 요청 실패 0건, 늦은 낙찰 0건, p99 ≤ 200ms |
| `검증-04` | 500 RPS, 예산 경합·중복·역순·입찰 응답 유실 | 예산 초과·중복 과금·고아 잠재 지출 0건, 최종 결과 일치 |
| `검증-05` | 500 RPS 2분 → 1,000 RPS 60초 → 100 RPS 60초 | 순간 부하 p99 ≤ 180ms, 30초 안에 회복, 정확성 유지 |
| `검증-06` | SSP·자사 DSP 인스턴스 중단 | 성공 응답 직후 종료해도 경매 결과·렌더링·최종 정산 복구, 10초 안에 회복, 금액 유실·중복 0건 |
| `검증-07` | AZ 하나 중단 | 전체 중단 0, 30초 안에 회복, 금액 RPO 0 |
| `검증-08` | 400 RPS 순차 배포 | 전체 중단·잘못된 낙찰·금액 불일치 0건 |
| `검증-09` | 렌더링 인증·60초 경계·최종 정산 | 위조 과금 0건, 경계 판정 100%, DSP 접수 p99 ≤ 5초, 충돌 격리 |

## 8. 결과에 반드시 남길 정보

커밋·빌드 식별자, 리전·AZ·인스턴스·복제본·저장 구조, JVM 설정, 데이터 버전과 편향, 요청률·시간·장애 구성, p50/p95/p99, 실패·누락·잘못된 낙찰, 최종 정산 적체와 전달 지연, CPU·메모리·GC·대기열·연결·처리 중 작업, 장애·복구 시각, 시험 전후 예산 대조 결과를 남긴다.
