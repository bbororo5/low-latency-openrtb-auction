# 아키텍처 동인

상태: 다섯 관점과 정산 계약 반영

근거:

- [아키텍처 중요 요구사항](asr.md)
- [제품·도메인 요구사항](../requirements/product-domain-requirements.md)
- [부하·데이터·검증 기준](../requirements/workload-data-verification.md)
- [데이터 중심](reviews/data-intensive-systems-review.md), [도메인 경계](reviews/domain-boundaries-review.md), [진화적 아키텍처](reviews/evolutionary-architecture-review.md), [운영·실패](reviews/operations-failure-review.md), [신뢰 경계](reviews/trust-boundaries-review.md) 검토

## 1. 핵심 동인

| ID | 동인 | 근거 |
|---|---|---|
| `동인-01` | 제한 시간 안에 정확하고 결정적인 경매 결과를 내야 한다. | `ASR-01`, `ASR-02` |
| `동인-02` | 모든 잠재 지출을 권위 있는 과금·미과금 결과로 한 번만 수렴시켜야 한다. | `ASR-04` |
| `동인-03` | 인스턴스와 AZ 장애에도 서비스를 지속하며 외부에 성공으로 인정한 사실을 잃지 않아야 한다. | `ASR-05` |
| `동인-04` | 외부 지연, 내부 자원 고갈, 재시도와 순간 부하가 전체 장애와 장기 지연으로 번지지 않아야 한다. | `ASR-02`, `ASR-03` |
| `동인-05` | 데이터의 원본성·수명·정합성·복구 기준을 구분하고 각 상태의 소유자를 명확히 해야 한다. | `ASR-01`, `ASR-04`, `ASR-05` |
| `동인-06` | 분산된 흐름을 재현·대조하고 통신·저장 계약을 순차 배포 중에도 호환되게 바꿀 수 있어야 한다. | `ASR-06`, `ASR-07` |
| `동인-07` | 외부 입력은 신원·연결·기한·중복을 검증한 뒤에만 낙찰과 금액 사실이 될 수 있다. | `ASR-08` |

## 2. 데이터 상태와 불변식

| 상태 | 성격 | 보존 기준 |
|---|---|---|
| 경매 진행 상태 | 제한 시간 안에서만 유효한 작업 상태 | 유실 시 해당 경매는 실패할 수 있지만 늦은 결과를 만들 수 없다. |
| 공개된 경매 결과 | 공급자 응답과 정산의 근거 | 동일 경매의 낙찰 입찰이 장애 뒤에 바뀌면 안 된다. |
| 렌더링 통지 | 과금 여부를 주장하는 외부 입력 | 최초 접수 시각과 낙찰 연결·검증 근거를 복구할 수 있어야 한다. |
| 최종 정산과 전달 상태 | SSP가 소유하는 과금·미과금 사실 | DSP가 복구 가능하게 접수할 때까지 결과와 전달 책임을 잃으면 안 된다. |
| 캠페인 실행 데이터 | 원본 시스템에서 공급되는 조회용 파생 데이터 | 데이터 버전별로 결정적이며 재구축할 수 있어야 한다. |
| 잠재·확정 지출 | 자사 DSP가 소유하는 금액 원본 | 동시 변경과 장애에서도 광고주·캠페인 한도를 함께 지켜야 한다. |
| 지표와 로그 | 검증용 파생 데이터 | 금액 원본을 대신하지 않으며 경계 간 흐름을 추적할 수 있어야 한다. |

```text
광고주 확정 지출 + 잠재 지출 <= 충전액
캠페인 누적 확정 지출 + 잠재 지출 <= 누적 한도
캠페인 일일 확정 지출 + 잠재 지출 <= 일일 한도
```

- 동일 경매·노출은 최대 한 건의 낙찰 입찰과 과금 결과만 만들며 중복 요청은 새 캠페인과 예약을 만들지 않는다.
- 입찰 응답으로 사용된 잠재 지출은 SSP 미접수·패찰·과금·미과금 중 하나로 반드시 종료되어야 한다.
- 같은 금액 사건은 여러 번 전달될 수 있지만 금액 효과는 한 번만 발생한다.
- 과금은 잠재 지출을 확정 지출로 바꾸며 사용 가능 예산을 다시 줄이지 않는다.
- 과금이 늦게 도착해도 경매 수신 시각의 날짜와 원래 낙찰가를 사용한다.
- DSP는 SSP의 최종 결과 없이 시간만으로 잠재 지출을 해제하지 않는다.
- 늦은 입찰은 경매 결과를 바꾸지 않는다.

## 3. 책임 경계

| 경계 | 소유하는 의미 |
|---|---|
| SSP 경매·정산 | 요청 검증, DSP 응답 판정, 마감, 낙찰, 렌더링 검증, 과금·미과금 확정과 결과 전달 |
| 외부 DSP 연동 | 외부 회사의 입찰 응답을 내부 경매 결과 후보로 변환하는 규칙 |
| 자사 DSP 입찰 | 실행 데이터, 후보 검색, 캠페인·소재 선택, 입찰가 |
| 자사 DSP 예산 | 잠재 지출, 확정 지출, 사용 가능 예산과 금액 상태 전이 |

렌더링은 공급자가 주장하고 SSP가 과금 여부를 확정하며 자사 DSP만 광고주·캠페인 예산을 바꾼다. 검증 자료는 도메인 경계가 아니라 모든 경계를 가로지르는 운영 관심사다.

SSP와 자사 DSP는 서로 다른 회사의 독립 배포·독립 저장소이며 계약으로만 통신한다. 각 회사 내부의 코드·프로세스·저장 구조는 이후 결정한다.

## 4. 변화·실패·신뢰 원칙

- JSON에서 이진 표현으로 바꾸거나 복제본을 늘려도 경매와 금액 의미는 바뀌지 않는다.
- 순차 배포 중 이전 버전과 새 버전의 통신·사건·저장 상태가 함께 동작해야 한다.
- 외부 DSP 하나의 실패는 해당 입찰만 제외하며 다른 DSP와 경매 전체로 퍼지지 않는다.
- 처리 능력을 넘으면 제한된 요청을 실패시키되 잘못된 낙찰과 금액 위반을 만들지 않는다.
- 공급자 역할은 렌더링을 주장할 뿐 과금을 확정하지 못한다.
- 렌더링은 SSP가 보존한 낙찰 결과와 대조하며 낙찰 후 60초 안에 복구 가능하게 접수된 통지만 인정한다.
- DSP가 제공한 `nurl`, `lurl`, `burl` 중 사전 등록된 주소만 비동기로 호출한다.
- SSP와 DSP의 성공 응답은 장애 후에도 복구 가능한 접수를 뜻한다.
- 송신자를 확인한 뒤에도 식별자, 권한, 현재 상태, 중복과 허용 시간을 검증한다.
- SSP는 모순된 최종 결과를 만들지 않으며 DSP는 계약 위반 결과를 금액에 다시 반영하지 않고 격리한다.

## 5. 충돌과 우선순위

1. 잘못된 낙찰과 금액 위반을 막는 것이 처리량보다 우선한다.
2. 금액 상태를 보존하기 위해서도 경매 제한 시간을 넘길 수는 없다.
3. 금액 저장 결과가 불확실하면 성공으로 응답하거나 입찰에 사용하지 않는다.
4. 가용성을 높이는 결정이 성공으로 인정한 금액 효과의 RPO 0을 약화해서는 안 된다.
5. 성능 최적화와 통신 변경은 도메인 의미와 자동 검증 기준을 바꾸지 않는 범위에서만 허용한다.

## 6. ADR로 넘길 결정 질문

- 경매 시간 예산과 외부 호출, 자원 상한, 과부하 거절을 어떻게 제어할 것인가?
- 캠페인 실행 데이터를 어떤 계약과 버전으로 공급·보관·조회할 것인가?
- CPM과 예산을 어떤 정밀도로 표현하고 광고주·캠페인 동시 지출을 어떻게 조정할 것인가?
- 입찰 응답이 유실되어도 모든 잠재 지출을 어떻게 권위 있는 결과로 종료할 것인가?
- 과금·미과금 결과를 통지·조회·대조 중 어떤 계약으로 전달하고 재시도할 것인가?
- 공개된 경매 결과, 렌더링 근거, 최종 정산과 전달 상태를 어디에 보존하고 장애에서 어떻게 복구할 것인가?
- 렌더링 통지와 DSP 제공 주소의 신원·권한·내용·재전송을 어떻게 검증할 것인가?
- 순차 배포 중 통신·사건·저장 계약의 호환성을 어떻게 유지할 것인가?
- 경매·금액 의미를 통신과 관측 도구에서 어떻게 분리하고 합격 기준을 자동 판정할 것인가?

각 질문의 대안과 트레이드오프를 ADR에서 결정한다.
