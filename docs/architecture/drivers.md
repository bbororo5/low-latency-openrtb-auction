# 아키텍처 동인

상태: 확정안

근거: [아키텍처 중요 요구사항](../requirements/quality.md), [제품·도메인 요구사항](../requirements/product.md), [부하·데이터·검증 기준](../requirements/workload.md)

관점별 근거: [데이터](rationale/data.md), [도메인](rationale/domain.md), [진화](rationale/evolution.md), [운영·실패](rationale/failure.md), [신뢰](rationale/trust.md)

## 1. 핵심 동인

| ID | 동인 | 근거 |
|---|---|---|
| `동인-01` | 외부 호출, 캠페인 검색과 금액 예약을 포함한 경매를 p99 50ms 안에 끝내야 한다. | `ASR-01` |
| `동인-02` | 외부 실패와 초과 부하를 격리하고 최소 400 RPS의 지연과 정확성을 보호해야 한다. | `ASR-02` |
| `동인-03` | 분산된 DSP 인스턴스가 같은 캠페인 총예산을 초과하지 않고 잠재 지출을 95초 안에 수렴시켜야 한다. | `ASR-03` |
| `동인-04` | 리전 간 실시간 합산 없이 각 리전의 책임액과 단기 리스로 캠페인 기간의 예산 소비 속도를 제어해야 한다. | `ASR-04` |
| `동인-05` | 인스턴스·게이트웨이·AZ·리전 장애를 부분 영향으로 격리하고 독립 장부가 모순 없는 금액 결과로 수렴해야 한다. | `ASR-05` |
| `동인-06` | 신뢰하지 않는 경계를 넘는 입찰·렌더링·금액 사건의 상대·연결·기한·중복을 검증해야 한다. | `ASR-06` |
| `동인-07` | 분산 흐름을 재현·설명하고 통신·저장·배포 방식을 도메인 의미와 독립적으로 바꿀 수 있어야 한다. | `ASR-07`, `ASR-08` |

## 2. 데이터와 불변식

| 데이터 | 소유자와 성격 | 보존 기준 |
|---|---|---|
| 경매 진행 상태 | SSP의 짧은 작업 상태 | 유실 시 요청은 실패할 수 있지만 180ms 뒤 결과를 만들지 않는다. |
| 렌더링 증표 | SSP가 낙찰과 렌더링을 연결하는 근거 | 서버 저장 없이 낙찰·가격·DSP·60초 기한의 위조 여부를 검증할 수 있어야 한다. |
| 렌더링 접수 | SSP가 검증할 외부 주장 | 최초 접수 시각과 낙찰 연결 근거를 복구해야 한다. |
| 통지 전달 상태 | SSP가 소유하는 비동기 작업 | DSP가 복구 가능하게 접수하거나 계약상 기한이 끝날 때까지 보존한다. |
| 캠페인 실행 데이터 | 프로젝트 DSP의 조회용 데이터 | 시험 전에 확정한 같은 버전이 두 리전의 인스턴스에 일관되게 배포된다. |
| 잠재·확정 지출 | 프로젝트 DSP의 금액 원본 | 캠페인 한도, 멱등 처리와 95초 만료를 복구할 수 있어야 한다. |
| 지표와 로그 | 검증용 파생 데이터 | 금액 원본을 대신하지 않고 흐름을 연결할 수 있어야 한다. |

```text
캠페인 확정 지출 + 유효한 잠재 지출 <= 캠페인 총예산
```

- 한 슬롯의 입찰은 하나의 잠재 지출을 만든다.
- 같은 캠페인이 여러 슬롯에 입찰하면 합계로 한도를 검사한다.
- 같은 논리 사건은 반복 전달되어도 금액 효과가 한 번만 발생한다.
- DSP 예약 전에 성공한 입찰은 없으며 예약 시각이 95초 기한의 기준이다.
- 처리 주체·인스턴스·배치가 바뀌어도 이미 만든 잠재 지출은 95초 수명주기를 그대로 따른다.
- 만료 전 `lurl`은 해제하고 `burl`은 확정하며, 사건이 없으면 만료 시 해제한다.
- 늦은 `burl`과 모순된 사건은 자동으로 금액을 다시 바꾸지 않는다.

## 3. 책임 경계

| 경계 | 책임 |
|---|---|
| SSP 경매 | 요청·DSP 응답 검증, 180ms 마감, 슬롯별 1가격 낙찰과 결과 보존 |
| SSP 렌더링·통지 | 렌더링 연결과 60초 판정, `nurl`·`lurl`·`burl`의 멱등 전달 |
| 외부 DSP 연동 | 외부 회사의 응답을 내부 경매 후보로 변환하고 실패를 격리 |
| 프로젝트 DSP 입찰 | 실행 데이터, 후보 검색, 페이싱, 캠페인·소재와 가격 선택 |
| 프로젝트 DSP 예산 | 잠재·확정 지출, 총예산 불변식과 95초 수명주기 |

SSP와 프로젝트 DSP는 서로 다른 회사의 독립 저장소를 사용한다. SSP는 과금 가능 렌더링을 자기 수취 근거로 확정하고 프로젝트 DSP가 입찰 응답에 넣은 공개 HTTP 주소로 `burl`을 보낸다. 프로젝트 DSP가 95초 안에 이를 복구 가능하게 접수할 때 광고주 캠페인 지출과 SSP 지급 근거가 확정된다. 양쪽은 상대 내부 장부를 읽거나 호출하지 않고 같은 사건의 멱등 재시도로 응답 유실을 해소한다. 청구서·송금·매체사 지급은 범위 밖이며 정상 기한 안의 양사 근거만 시험에서 대조한다.

## 4. 실패와 변화 원칙

- 외부 DSP 하나의 지연은 해당 입찰에서 끝난다.
- 기술 실패를 광고 없음이나 정상 `NO_BID`로 숨기지 않는다.
- 금액 경로가 불확실해도 예산 안전성을 증명할 수 있는 범위와 복구 가능한 과금 근거가 있으면 그 범위에서 프로젝트 DSP의 입찰을 계속한다. 둘 중 하나라도 없으면 프로젝트 DSP의 입찰을 중단하되 SSP는 외부 DSP와 경매를 계속한다.
- 인스턴스 수와 장애 영역이 바뀌어도 낙찰, 가격, 예약과 과금 의미는 바뀌지 않는다.
- JSON에서 이진 표현으로 바꾸어도 같은 합격 시험을 사용한다.
- 이전 버전과 새 버전이 함께 동작하는 동안 통신과 저장 상태가 호환되어야 한다.
- 정확히 한 번 전달을 가정하지 않고 여러 번 전달되어도 한 번만 반영한다.

## 5. ADR 목록

구성요소, 상태 소유권, 통신 경로 또는 장애 전파 방식이 달라지는 결정만 ADR로 기록한다.

| ADR | 상태 | 결정 |
|---|---|---|
| [ADR-001](decisions/ADR-001-distributed-budget-reservation.md) | 승인 | 계층형 에스크로 예산과 DSP 로컬 예약 |
| [ADR-002](decisions/ADR-002-multi-region-ledger-topology.md) | 승인 | 강한 리전 책임 분리와 리전별 독립 원장 |
| [ADR-003](decisions/ADR-003-regional-budget-allocation.md) | 승인 | 초기 리전 책임액·전역 예비액과 입찰 경로 밖의 요청형 이전 |
| [ADR-004](decisions/ADR-004-auction-execution-path.md) | 승인 | 기한·상한·DSP별 격리가 있는 병렬 경매 실행 경로 |
| [ADR-005](decisions/ADR-005-durable-budget-events.md) | 승인 | 독립 지역 장부, 내구 접수 증거와 멱등 금액 사건 수렴 |
| [ADR-006](decisions/ADR-006-multi-region-service-topology.md) | 승인 | 두 리전 능동 실행과 리전별 독립 장애 격리 |

다음은 ADR이 아니라 상세 설계·설정·검증에서 정한다.

- DSP 권한 크기, 리스 기간과 보충 임계치
- 캠페인 조회 구조, 선택 규칙과 페이싱 계산 주기
- 서버 인증, 증표 형식과 키 관리
- JSON·이진 표현, 버전 호환과 순차 배포 절차
- 지표·로그·추적 항목과 자동 합격 판정

상세 설계 중 새로운 구조적 대안과 되돌리기 어려운 트레이드오프가 드러날 때만 ADR을 추가한다.
