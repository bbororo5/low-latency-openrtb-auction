services:
  prometheus:
    volumes:
      - ./monitoring/prometheus/prometheus.cloud.yml:/etc/prometheus/prometheus.yml:ro
      - ./.secrets/grafana_cloud_api_token:/run/secrets/grafana_cloud_api_token:ro

  grafana:
    profiles:
      - local-grafana
